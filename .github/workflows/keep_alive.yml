name: Keep bot alive

on:
  schedule:
    - cron: '*/5 * * * *'  # 5分ごとにping

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping bot
        run: curl -s "${{ secrets.BOT_URL }}" || true
